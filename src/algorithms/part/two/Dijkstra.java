package algorithms.part.two;

public class Dijkstra {
//
//    private final DirectedEdge[] edgeTo;
//    private final double[] distanceTo;
//    MinHeapTree<DirectedEdge> edges;
//
//    public Dijkstra(EdgeWeightedDirectedGraph edgeWeightedDirectedGraph) {
//        int noOfVertices = edgeWeightedDirectedGraph.noOfVertices();
//        edgeTo = new DirectedEdge[noOfVertices];
//        distanceTo = new double[noOfVertices];
//        edges = new MinHeapTree<>();
//        Arrays.fill(distanceTo, Double.MAX_VALUE);
//        edgeTo[0] = null;
//        distanceTo[0] = 0;
//        visit(edgeWeightedDirectedGraph, 0);
//        while ()
//    }
//
//    private void visit(EdgeWeightedDirectedGraph edgeWeightedDirectedGraph, int vertex) {
//        for(DirectedEdge directedEdge : edgeWeightedDirectedGraph.edges())
//            edges.insert(directedEdge);
//    }
//
//    private void relaxEdge(DirectedEdge directedEdge) {
//        int from = directedEdge.from(), to = directedEdge.to();
//        if(distanceTo[to] > distanceTo[from] + directedEdge.weight()) {
//            distanceTo[to] = distanceTo[from] + directedEdge.weight();
//            edgeTo[to] = directedEdge;
//        }
//    }

}
